---
language: node_js

node_js:
  - 'iojs'
  - '0.12'
  - '0.10'

sudo: false

cache:
  directories:
    - node_modules

before_install:

install:
  - npm install -g bower
  - npm install
  - bower install

script:
  - npm test
